import { Text } from '@chakra-ui/react';
import type { NextPage } from 'next';
import { Layout } from '../components/Layout';

const Home: NextPage = () => {
  return (
    <Layout>
      <Text>React 18 Sample</Text>
    </Layout>
  );
};

export default Home;
